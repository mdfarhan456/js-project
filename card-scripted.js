var options = {
    strings: ["Welcome to Our Doctor Booking Platform!", "Choose your preferred doctor and book now."],
    typeSpeed: 50,  // speed of typing
    backSpeed: 25,  // speed of backspacing
    backDelay: 1500,  // delay before backspacing
    startDelay: 500,  // delay before typing starts
    loop: true , // repeat the animation
    showCursor: false
  };
  
  var typed = new Typed("#typed-output", options);
  

  var options = {
    strings: ["Your Health, Our Priority"],
    typeSpeed: 50,  // speed of typing
    backSpeed: 25,  // speed of backspacing
    backDelay: 1500,  // delay before backspacing
    startDelay: 500,  // delay before typing starts
    loop: true , // repeat the animation
    showCursor:false
  };
  var typed = new Typed(".overlay-text2", options);